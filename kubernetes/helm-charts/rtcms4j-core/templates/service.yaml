apiVersion: v1
kind: Service
metadata:
  name: {{ include "rtcms4j-core.fullname" . }}
  labels:
    {{- include "rtcms4j-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rtcms4j-core.selectorLabels" . | nindent 4 }}
